# Руководство по Деплою (MarsSpace)

Этот гид поможет вам развернуть проект MarsSpace на продакшен-серверы. Мы рекомендуем использовать **Render.com** для бэкенда (Django + PostgreSQL) и **Vercel** для фронтенда (React).

## 📋 Предварительные требования

Перед началом убедитесь, что у вас есть:
1.  Аккаунты на [GitHub](https://github.com/), [Render](https://render.com/) и [Vercel](https://vercel.com/).
2.  Установленный Git.
3.  Проект загружен в репозиторий GitHub.

---

## 🚀 Часть 1: Бэкенд (Django) на Render

### 1. Подготовка репозитория
В корне проекта создайте файл `build.sh` (если его нет) со следующим содержимым:
```bash
#!/usr/bin/env bash
# exit on error
set -o errexit

pip install -r backend/requirements.txt

python backend/manage.py collectstatic --no-input
python backend/manage.py migrate
```
Убедитесь, что файл исполняемый: `chmod a+x build.sh` (на Windows можно пропустить, Render сам сделает `chmod`).

### 2. Создание сервиса на Render
1.  В Render Dashboard нажмите **New +** -> **Web Service**.
2.  Подключите ваш репозиторий.
3.  Заполните форму:
    -   **Name**: `marsspace-backend`
    -   **Region**: Frankfurt (или любой ближе к вам)
    -   **Branch**: `main` (или ваша ветка)
    -   **Root Directory**: `.` (оставьте пустым)
    -   **Runtime**: Python 3
    -   **Build Command**: `./build.sh`
    -   **Start Command**: `gunicorn backend.config.wsgi:application --chdir ./backend`

### 3. Переменные окружения (Environment Variables)
На вкладке **Environment** добавьте следующие ключи:

| Key | Value | Описание |
| :--- | :--- | :--- |
| `PYTHON_VERSION` | `3.10.0` (или ваша версия) | Версия Python |
| `SECRET_KEY` | `сгенерируйте_длинный_случайный_ключ` | Ключ шифрования Django |
| `DEBUG` | `False` | **Важно!** Выключаем режим отладки |
| `ALLOWED_HOSTS` | `*.onrender.com` | Разрешаем хосты Render |
| `CORS_ALLOWED_ORIGINS` | `https://ваш-фронтенд.vercel.app` | Добавите позже, когда развернете фронтенд |
| `DATABASE_URL` | *(будет создан автоматически)* | См. шаг 4 |

### 4. Подключение базы данных (PostgreSQL)
1.  В Render Dashboard нажмите **New +** -> **PostgreSQL**.
2.  Назовите его `marsspace-db`.
3.  После создания скопируйте **Internal Database URL**.
4.  Вернитесь в ваш Web Service -> Environment и добавьте переменную `DATABASE_URL` со скопированным значением.

---

## ⚛️ Часть 2: Фронтенд (React) на Vercel

### 1. Создание проекта
1.  В Vercel Dashboard нажмите **Add New...** -> **Project**.
2.  Импортируйте ваш репозиторий.

### 2. Настройка сборки
Vercel обычно сам определяет настройки для Vite, но проверьте:
-   **Framework Preset**: Vite
-   **Root Directory**: `frontend` (Нажмите "Edit" и выберите папку frontend)
-   **Build Command**: `npm run build` (стандартная)
-   **Output Directory**: `dist` (стандартная)
-   **Install Command**: `npm install` (стандартная)

### 3. Переменные окружения
Разверните секцию **Environment Variables** и добавьте:

| Key | Value |
| :--- | :--- |
| `VITE_API_URL` | `https://marsspace-backend.onrender.com/api/v1` |

*Внимание: URL бэкенда вы получите после его деплоя на Render. Не забудьте добавить `/api/v1` в конце, если ваши эндпоинты начинаются с этого пути.*

### 4. Деплой
Нажмите **Deploy**. После успешной сборки вы получите домен (например, `marsspace-frontend.vercel.app`).

---

## 🔗 Часть 3: Финальная связка

1.  Скопируйте домен вашего фронтенда из Vercel (например, `https://marsspace-frontend.vercel.app`).
2.  Перейдите в Render -> Web Service -> Environment.
3.  Отредактируйте `CORS_ALLOWED_ORIGINS` и вставьте туда этот домен.
4.  Render автоматически перезапустит бэкенд.

## ✅ Готово!
Теперь ваш проект доступен по ссылке из Vercel, бэкенд работает на Render, а база данных — в PostgreSQL облаке.
