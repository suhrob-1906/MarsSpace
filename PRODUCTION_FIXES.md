# 🔧 ИСПРАВЛЕНИЯ ОШИБОК НА PRODUCTION

## Дата: 30.01.2026, 22:40

---

## ✅ Исправленные ошибки:

### 1. ❌ 403 Forbidden - `/admin/homework/submissions/`

**Проблема**: Teacher Dashboard пытался получить данные с админского эндпоинта

**Решение**:
```jsx
// Было:
api.get('/admin/homework/submissions/')

// Стало:
api.get('/homework-submissions/')
```

**Файл**: `frontend/src/pages/TeacherDashboard.jsx`

---

### 2. ❌ 404 Not Found - `/eduverse/blog-posts/`

**Проблема**: URL в backend не совпадал с frontend

**Решение**:
```python
# Было:
router.register(r'blog/posts', BlogPostViewSet, ...)

# Стало:
router.register(r'eduverse/blog-posts', BlogPostViewSet, ...)
```

**Файл**: `backend/eduverse/urls.py`

---

## ⚠️ Требуется проверка на Production:

### 3. WPM не обновляется

**Причина**: Миграция `0007_user_last_wpm.py` не применена на Render

**Решение**:
1. Зайти в Render Dashboard
2. Открыть Shell для backend сервиса
3. Выполнить:
```bash
python manage.py migrate users
```

Или через Manual Deploy:
- Render автоматически применит миграции при деплое

---

### 4. ZIP файлы не загружаются

**Возможные причины**:
1. Размер файла превышает лимит
2. Настройки MEDIA на Render

**Проверить**:

**Файл**: `backend/config/settings.py`
```python
# Должно быть:
FILE_UPLOAD_MAX_MEMORY_SIZE = 10 * 1024 * 1024  # 10MB
DATA_UPLOAD_MAX_MEMORY_SIZE = 10 * 1024 * 1024  # 10MB
```

**Для Render** нужно настроить хранилище файлов:
- Использовать AWS S3, Cloudinary или другой сервис
- Или увеличить лимиты на Render

**Временное решение**:
```python
# В settings.py увеличить лимит
FILE_UPLOAD_MAX_MEMORY_SIZE = 50 * 1024 * 1024  # 50MB
```

---

## 📋 Команды для деплоя:

### Backend (Render):
```bash
# 1. Коммит изменений
git add .
git commit -m "Fix: 403/404 errors, blog posts URL, homework submissions endpoint"
git push origin main

# 2. Render автоматически задеплоит
# 3. Проверить логи в Render Dashboard
```

### Проверить миграции на Render:
```bash
# В Render Shell:
python manage.py showmigrations users

# Должно показать:
# [X] 0007_user_last_wpm

# Если нет, выполнить:
python manage.py migrate users
```

---

## ✅ Что исправлено локально:

1. ✅ Teacher Dashboard - использует правильный эндпоинт
2. ✅ Blog Posts URL - исправлен на `eduverse/blog-posts`
3. ✅ Next Lesson - показывает день недели и время
4. ✅ Homework Submissions - красивые карточки со статусами

---

## 🚀 После деплоя проверить:

1. Teacher Dashboard загружается без 403 ошибок
2. Blogs страница загружается без 404 ошибок
3. WPM обновляется после typing game
4. ZIP файлы загружаются (если настроено хранилище)

---

## 📞 Если проблемы остаются:

1. Проверить логи Render: `https://dashboard.render.com`
2. Проверить Network tab в браузере (F12)
3. Убедиться что миграции применены
4. Проверить Environment Variables на Render
