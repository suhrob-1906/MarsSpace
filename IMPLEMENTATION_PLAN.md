# MarsSpace Platform - Comprehensive Upgrade Plan

## Цель проекта

Модернизировать образовательную платформу MarsSpace, добавив систему подписок, AI-чат, улучшенную систему геймификации с рейтингами, и расширенные возможности для администраторов и учителей. Проект должен оставаться простым в развертывании без Docker и Redis.

---

## Текущая архитектура

### Backend (Django + DRF)
- **Users**: Роли (ADMIN, TEACHER, STUDENT), группы обучения
- **Game**: Typing game, сезоны, кошелек (coins, energy, streak)
- **Shop**: Товары, заказы
- **Eduverse**: Видео-курсы, категории, блог
- **Auth**: JWT токены через SimpleJWT

### Frontend (React + Vite + Tailwind)
- Dashboard, Courses, Shop, TypingGame, Blog
- AdminDashboard, TeacherDashboard
- JWT authentication

---

## Предлагаемые изменения

### 1. Система пользователей и аутентификации

#### Backend изменения

**Модель User (users/models.py)**
```python
class User(AbstractUser):
    # Существующие поля
    role = models.CharField(...)
    language = models.CharField(...)
    
    # НОВЫЕ ПОЛЯ
    coins = models.IntegerField(default=0)  # Перенести из Wallet
    points = models.IntegerField(default=0)  # Очки за рейтинг
    activity_days = models.IntegerField(default=0)  # Дни активности
    has_premium = models.BooleanField(default=False)  # Премиум подписка
    premium_expires_at = models.DateTimeField(null=True, blank=True)
    avatar_url = models.CharField(max_length=500, blank=True)
    last_activity_date = models.DateField(auto_now=True)
```

**Новая модель Homework (eduverse/models.py)**
```python
class Homework(models.Model):
    title = models.CharField(max_length=200)
    description = models.TextField()
    course_category = models.ForeignKey(EduverseCategory, on_delete=models.CASCADE)
    due_date = models.DateTimeField()
    max_points = models.IntegerField(default=100)
    created_by = models.ForeignKey(User, on_delete=CASCADE)
    
class HomeworkSubmission(models.Model):
    homework = models.ForeignKey(Homework, on_delete=CASCADE)
    student = models.ForeignKey(User, on_delete=CASCADE)
    content = models.TextField()
    file_url = models.CharField(max_length=500, blank=True)
    points_earned = models.IntegerField(default=0)
    submitted_at = models.DateTimeField(auto_now_add=True)
    graded_by = models.ForeignKey(User, null=True, on_delete=SET_NULL)
```

**Новые API endpoints**
- `POST /api/v1/subscription/purchase/` - Купить премиум за 100 коинов
- `GET /api/v1/subscription/status/` - Проверить статус подписки
- `POST /api/v1/ai-chat/` - Отправить сообщение AI (интеграция с Gemini API)
- `GET /api/v1/homework/` - Список домашних заданий
- `POST /api/v1/homework/submit/` - Отправить ДЗ

---

### 2. Студенческий интерфейс

#### Новый Header компонент
```
┌─────────────────────────────────────────────────────────┐
│ [Logo] MarsSpace    🔥 15 дней  💰 450  ⭐ 1250  [👤]   │
└─────────────────────────────────────────────────────────┘
```

**Элементы:**
- Логотип слева
- Активность в днях (activity_days)
- Коины (coins)
- Очки (points)
- Круглый аватар справа → клик открывает меню:
  - Имя Фамилия
  - Язык (RU/UZ переключатель)
  - Выход

#### Блок подписки (на Dashboard)
```
┌──────────────────────────────────────────┐
│  💎 Премиум подписка                     │
│  Получи доступ к эксклюзивным курсам     │
│  [Купить за 100 💰] или [Премиум ✓]      │
└──────────────────────────────────────────┘
```

- Если `has_premium = false` → кнопка "Купить за 100 💰"
- Если `has_premium = true` → бадж "Премиум ✓"

#### Typing Game с рейтингом

**Новая логика:**
1. После игры сохраняется результат (WPM, accuracy, score)
2. Каждый сезон (Season) имеет таблицу лидеров
3. В конце сезона топ-3 получают очки:
   - 1 место: 300 points
   - 2 место: 150 points
   - 3 место: 75 points
4. Очки конвертируются в коины автоматически

**API:**
- `GET /api/v1/game/leaderboard/` - Топ игроков текущего сезона
- `POST /api/v1/game/typing-attempt/` - Сохранить результат игры

#### AI Chat Widget (правый нижний угол)

**Компонент AIChatWidget.jsx**
```jsx
<div className="fixed bottom-4 right-4 z-50">
  {isOpen ? (
    <div className="w-80 h-96 bg-white rounded-lg shadow-xl">
      {/* Чат интерфейс */}
    </div>
  ) : (
    <button className="w-14 h-14 rounded-full bg-blue-500">
      💬
    </button>
  )}
</div>
```

**Backend интеграция:**
```python
# users/views.py
import google.generativeai as genai

class AIChatView(APIView):
    def post(self, request):
        message = request.data.get('message')
        genai.configure(api_key=settings.GEMINI_API_KEY)
        model = genai.GenerativeModel('gemini-pro')
        response = model.generate_content(message)
        return Response({'reply': response.text})
```

#### Sidebar навигация

```
┌─────────────┐
│ 📚 Курсы    │
│ 🎓 Eduverse │
│ 📝 Блог     │
│ 🛒 Магазин  │
└─────────────┘
```

- Курсы → Eduverse видео-уроки
- Eduverse → То же самое (можно объединить)
- Блог → Существующий BlogPost
- Магазин → Существующий Shop

---

### 3. Интерфейс учителя

**TeacherDashboard обновления:**

```
┌────────────────────────────────────────────┐
│  👨‍🏫 Мои группы                            │
│  ┌──────────────────────────────────────┐  │
│  │ Группа Python 2024                   │  │
│  │ Студенты: 15                         │  │
│  │ [Посещаемость] [Прогресс]            │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

**Новые возможности:**
- Просмотр списка студентов группы
- Отметка посещаемости (новая модель Attendance)
- Просмотр прогресса студентов (пройденные уроки, очки)

**Новая модель Attendance:**
```python
class Attendance(models.Model):
    student = models.ForeignKey(User, on_delete=CASCADE)
    group = models.ForeignKey(StudyGroup, on_delete=CASCADE)
    date = models.DateField()
    is_present = models.BooleanField(default=False)
    marked_by = models.ForeignKey(User, related_name='marked_attendance', on_delete=CASCADE)
```

**API:**
- `GET /api/v1/teacher/groups/` - Группы учителя
- `POST /api/v1/teacher/attendance/` - Отметить посещаемость
- `GET /api/v1/teacher/students/{group_id}/` - Студенты группы

---

### 4. Админ Dashboard

**Полностью новый AdminDashboard.jsx**

```
┌─────────────────────────────────────────────────────┐
│  👑 Панель администратора                           │
│  ┌──────────┬──────────┬──────────┬──────────┐      │
│  │ Группы   │ Учителя  │ Товары   │ Курсы    │      │
│  └──────────┴──────────┴──────────┴──────────┘      │
│                                                      │
│  [+ Создать группу]                                 │
│  ┌────────────────────────────────────────┐         │
│  │ Группа: Python 2024                    │         │
│  │ Учитель: Иван Иванов                   │         │
│  │ Студенты: 15                           │         │
│  │ [Редактировать] [Удалить]              │         │
│  └────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────┘
```

**Функции админа:**

1. **Управление группами**
   - Создать группу (название, описание)
   - Назначить учителя
   - Добавить студентов

2. **Управление пользователями**
   - Создать учителя (имя, фамилия, логин, пароль)
   - Создать студента
   - Изменить роль пользователя

3. **Управление товарами (Shop)**
   - Добавить товар (название, описание, цена в коинах, картинка)
   - Редактировать товары
   - Удалить товары

4. **Управление курсами**
   - Добавить категорию курса
   - Добавить видео-урок (название, URL видео, баннер)
   - Редактировать уроки

5. **Управление ДЗ**
   - Создать домашнее задание
   - Назначить группе
   - Проверить сданные работы

**API для админа:**
- `POST /api/v1/admin/groups/` - Создать группу
- `POST /api/v1/admin/users/` - Создать пользователя
- `PUT /api/v1/admin/users/{id}/` - Изменить пользователя
- `POST /api/v1/admin/shop-items/` - Добавить товар
- `POST /api/v1/admin/courses/` - Добавить курс
- `POST /api/v1/admin/homework/` - Создать ДЗ

---

## Структура файлов

### Backend новые/измененные файлы

```
backend/
├── users/
│   ├── models.py (обновить User, добавить Attendance)
│   ├── serializers.py (новые сериализаторы)
│   ├── views.py (AI chat, subscription)
│   └── urls.py
├── eduverse/
│   ├── models.py (добавить Homework, HomeworkSubmission)
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── game/
│   ├── models.py (обновить Season rewards)
│   ├── views.py (leaderboard logic)
│   └── urls.py
└── config/
    └── settings.py (добавить GEMINI_API_KEY)
```

### Frontend новые/измененные файлы

```
frontend/src/
├── components/
│   ├── Header.jsx (новый)
│   ├── SubscriptionCard.jsx (новый)
│   ├── AIChatWidget.jsx (новый)
│   ├── Sidebar.jsx (обновить)
│   └── AccountDropdown.jsx (новый)
├── pages/
│   ├── Dashboard.jsx (обновить с новым header)
│   ├── TypingGame.jsx (добавить leaderboard)
│   ├── TeacherDashboard.jsx (добавить attendance)
│   └── AdminDashboard.jsx (полностью переделать)
└── services/
    ├── api.js (новые endpoints)
    └── aiChat.js (новый)
```

---

## Миграции данных

### Шаг 1: Перенос данных из Wallet в User
```python
# users/management/commands/migrate_wallet_to_user.py
from django.core.management.base import BaseCommand
from users.models import User
from game.models import Wallet

class Command(BaseCommand):
    def handle(self, *args, **kwargs):
        for wallet in Wallet.objects.all():
            user = wallet.student
            user.coins = wallet.coins
            user.activity_days = wallet.streak
            user.save()
```

### Шаг 2: Создать начальный сезон
```python
# game/management/commands/create_season.py
from game.models import Season
from datetime import date, timedelta

Season.objects.create(
    title="Сезон 1 - 2026",
    start_date=date.today(),
    end_date=date.today() + timedelta(days=90),
    rewards_json={"1": 300, "2": 150, "3": 75},
    is_active=True
)
```

---

## План верификации

### 1. Backend тесты

**Тест подписки:**
```bash
cd backend
python manage.py test users.tests.SubscriptionTestCase
```

**Тест AI Chat:**
```bash
python manage.py test users.tests.AIChatTestCase
```

**Тест рейтинга:**
```bash
python manage.py test game.tests.LeaderboardTestCase
```

### 2. Frontend тесты

**Запуск dev сервера:**
```bash
cd frontend
npm run dev
```

**Ручное тестирование:**
1. Войти как студент → проверить новый header (коины, очки, дни)
2. Купить подписку → проверить что кнопка меняется на "Премиум ✓"
3. Сыграть в typing game → проверить что результат сохраняется
4. Открыть AI чат → отправить сообщение, получить ответ
5. Войти как учитель → отметить посещаемость
6. Войти как админ → создать группу, добавить товар

### 3. API тесты (Postman/curl)

```bash
# Купить подписку
curl -X POST http://localhost:8000/api/v1/subscription/purchase/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"

# AI Chat
curl -X POST http://localhost:8000/api/v1/ai-chat/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Как выучить Python?"}'

# Leaderboard
curl http://localhost:8000/api/v1/game/leaderboard/
```

---

## Что НЕ делаем (упрощение)

❌ **Docker** - не нужен, используем простой запуск через venv  
❌ **Redis** - не нужен, используем SQLite/PostgreSQL для всего  
❌ **Celery** - не нужен, все синхронно  
❌ **WebSockets** - AI чат работает через обычные HTTP запросы  

---

## Порядок реализации

### Фаза 1: Backend основа (2-3 дня)
1. Обновить модель User (добавить coins, points, premium)
2. Создать Homework модели
3. Создать Attendance модель
4. Написать миграции
5. Создать API endpoints для подписки

### Фаза 2: AI интеграция (1 день)
1. Добавить Gemini API в settings
2. Создать AIChatView
3. Протестировать API

### Фаза 3: Геймификация (2 дня)
1. Обновить TypingAttempt logic
2. Создать leaderboard endpoint
3. Автоматическое начисление очков топ-3

### Фаза 4: Frontend студент (3 дня)
1. Новый Header компонент
2. SubscriptionCard
3. AIChatWidget
4. Обновить Dashboard
5. Обновить TypingGame с leaderboard

### Фаза 5: Frontend учитель (2 дня)
1. Обновить TeacherDashboard
2. Attendance UI
3. Student progress view

### Фаза 6: Frontend админ (3 дня)
1. Полностью переделать AdminDashboard
2. CRUD для групп
3. CRUD для пользователей
4. CRUD для товаров
5. CRUD для курсов
6. CRUD для ДЗ

### Фаза 7: Тестирование (2 дня)
1. Unit тесты backend
2. Ручное тестирование всех фич
3. Исправление багов

---

## Итого

**Общее время:** ~15 дней разработки

**Ключевые улучшения:**
- ✅ Система подписок (100 коинов)
- ✅ AI чат с Gemini
- ✅ Рейтинговая система в typing game
- ✅ Посещаемость для учителей
- ✅ Полноценная админ-панель
- ✅ Домашние задания
- ✅ Улучшенный UX (header, sidebar, аватары)

**Технологии остаются простыми:**
- Django + DRF + SQLite
- React + Vite + Tailwind
- JWT auth
- Gemini API для AI

Проект остается легким в развертывании и поддержке! 🚀
